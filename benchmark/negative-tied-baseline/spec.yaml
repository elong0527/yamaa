schema_version: "1.0"
domain: ADLB
keys: [STUDYID, USUBJID, PARAMCD, LBSEQ]
input:
  LB: input/lb.csv
  ADSL: input/adsl.csv
base: LB

output:
  path: adlb.csv
  columns: [STUDYID, USUBJID, PARAMCD, LBSEQ, ADT, TRTSDT, AVAL, ABLFL, BASE]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation: LB.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: LB.USUBJID

  - name: PARAMCD
    type: str
    label: Parameter Code
    derivation: LB.LBTESTCD

  - name: LBSEQ
    type: int
    label: Sequence Number
    derivation: LB.LBSEQ

  - name: ADT
    type: date
    label: Analysis Date
    derivation: LB.LBDTC

  - name: TRTSDT
    type: date
    label: Date of First Exposure to Treatment
    derivation:
      lookup:
        dataset: ADSL
        value: TRTSDT
  - name: AVAL
    type: float
    label: Analysis Value
    derivation: LB.LBSTRESN

  - name: ABLFL
    type: str
    label: Baseline Record Flag
    derivation:
      baseline_flag:
        window:
          group_by: [STUDYID, USUBJID, PARAMCD]
        date: ADT
        reference_date: TRTSDT

  - name: BASE
    type: float
    label: Baseline Value
    derivation:
      aggregate:
        filter: "ABLFL = 'Y'"
        group_by: [STUDYID, USUBJID, PARAMCD]
        expr: "ONLY(AVAL)"
