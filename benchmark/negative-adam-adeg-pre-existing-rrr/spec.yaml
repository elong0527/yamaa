schema_version: "1.0"
domain: ADEG
keys: [STUDYID, USUBJID, PARAMCD, AVISIT]
input:
  ADEG_RAW:
    path: input/adeg.csv
    types: {AVAL: float}

output:
  path: adeg.csv
  columns: [STUDYID, USUBJID, PARAMCD, PARAM, AVISIT, AVAL, AVALU]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation: ADEG_RAW.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: ADEG_RAW.USUBJID

  - name: PARAMCD
    type: str
    label: Parameter Code

  - name: PARAM
    type: str
    label: Parameter

  - name: AVISIT
    type: str
    label: Analysis Visit
    derivation: ADEG_RAW.AVISIT

  - name: AVAL
    type: float
    label: Analysis Value

  - name: AVALU
    type: str
    label: Analysis Value Unit

  - name: _PROVENANCE
    type: str
    label: Internal Row Provenance

rows:
  - id: collected
    derivations:
      PARAMCD:
        source: ADEG_RAW.PARAMCD
      PARAM:
        source: ADEG_RAW.PARAM
      AVAL:
        source: ADEG_RAW.AVAL
      AVALU:
        source: ADEG_RAW.AVALU
      _PROVENANCE:
        literal: collected

  - id: derived_rrr
    filter: "ADEG_RAW.PARAMCD = 'HR' AND ADEG_RAW.AVAL IS NOT NULL AND ADEG_RAW.AVAL <> 0"
    derivations:
      PARAMCD:
        literal: RRR
      PARAM:
        literal: RR Duration Rederived (ms)
      AVAL:
        compute:
          expr: "60000 / ADEG_RAW.AVAL"
      AVALU:
        literal: ms
      _PROVENANCE:
        literal: derived

verifications:
  - unique:
      columns: [STUDYID, USUBJID, PARAMCD, AVISIT]
  - implies:
      id: contributing-hr-uses-beats-per-minute
      when: "PARAMCD = 'HR' AND AVAL IS NOT NULL AND AVAL <> 0"
      then: "AVALU = 'beats/min'"
  - implies:
      id: reject-pre-existing-rrr
      when: "PARAMCD = 'RRR'"
      then: "_PROVENANCE = 'derived'"
