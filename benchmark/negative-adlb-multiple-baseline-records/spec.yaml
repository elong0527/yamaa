schema_version: "1.0"
domain: ADLB
keys: [STUDYID, USUBJID, PARAMCD, ADT]
input:
  ADLB_RAW: {path: input/adlb_raw.csv, types: {AVAL: float, ADT: date}}

output:
  path: adlb.csv
  columns: [STUDYID, USUBJID, PARAMCD, ADT, AVAL, ABLFL]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation: ADLB_RAW.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: ADLB_RAW.USUBJID

  - name: PARAMCD
    type: str
    label: Parameter Code
    derivation: ADLB_RAW.PARAMCD

  - name: ADT
    type: date
    label: Analysis Date
    derivation: ADLB_RAW.ADT

  - name: AVAL
    type: float
    label: Analysis Value
    derivation: ADLB_RAW.AVAL

  - name: ABLFL
    type: str
    label: Baseline Record Flag
    derivation: ADLB_RAW.ABLFL
    verifications:
      allowed_values:
        values: [Y]

verifications:
  - unique:
      columns: [STUDYID, USUBJID, PARAMCD, ADT]
  - row_count:
      id: one-baseline-record-per-subject-and-parameter
      group_by: [STUDYID, USUBJID, PARAMCD]
      filter: "ABLFL = 'Y'"
      min: 1
      max: 1
