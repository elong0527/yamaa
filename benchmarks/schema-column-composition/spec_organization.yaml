schema_version: "1.0"
input:
  LB:
    path: input/lb.csv
    types:
      USUBJID: str
      LBTESTCD: str
      LBSTRESN: float
      LBNRIND: str
base: LB

columns:
  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: LB.USUBJID

  - name: PARAMCD
    type: str
    label: Parameter Code
    derivation: LB.LBTESTCD

  # The compound and study levels each add a metadata entry; the study
  # level also replaces the derivation and the whole verification list.
  - name: AVAL
    type: float
    label: Analysis Value
    derivation: LB.LBSTRESN
    verifications:
      - range:
          min: 0
    metadata:
      analysis_role: analysis-value

  # Later levels add an entry to dict and a missing handler; source and
  # case_sensitive are inherited from here unchanged.
  - name: ANRIND
    type: str
    label: Analysis Reference Range Indicator
    derivation:
      mapping:
        source: LB.LBNRIND
        dict:
          N: NORMAL
          H: HIGH
        case_sensitive: false

  # The study level replaces this mapping with a literal.
  - name: PARCAT1
    type: str
    label: Parameter Category
    derivation:
      mapping:
        source: LB.LBTESTCD
        dict:
          ALT: HEPATIC
          AST: HEPATIC

metadata:
  scope: organization
