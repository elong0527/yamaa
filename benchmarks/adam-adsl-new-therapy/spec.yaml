schema_version: "1.0"
domain: ADSL
keys: [STUDYID, USUBJID]
input:
  ADSL_RAW: input/adsl.csv
  CM: input/cm.csv
  PR: input/pr.csv
base: ADSL_RAW

output:
  path: adsl.csv
  columns: [STUDYID, USUBJID, TRTSDT, NACTDT, NACTDY, NACTFL]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation: ADSL_RAW.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: ADSL_RAW.USUBJID

  - name: TRTSDT
    type: date
    label: Date of First Exposure to Treatment
    derivation: ADSL_RAW.TRTSDT
    verifications:
      not_missing: {}

  # The first start of an on-treatment medication, and of an on-treatment
  # cancer-related procedure. MIN skips a missing date, so either is empty
  # when the subject has no dated qualifying record.
  - name: CMNACTDT
    type: date
    label: First New Anti-Cancer Medication Date
    derivation:
      aggregate:
        filter: "CM.CMCAT = 'ON TREATMENT'"
        expr: "MIN(CM.CMSTDTC)"

  - name: PRNACTDT
    type: date
    label: First New Anti-Cancer Procedure Date
    derivation:
      aggregate:
        filter: "PR.PRCAT = 'CANCER RELATED' AND PR.PRSCAT = 'ON TREATMENT'"
        expr: "MIN(PR.PRSTDTC)"

  # The earlier of the two, skipping an empty one; empty when both are.
  - name: NACTDT
    type: date
    label: New Anti-Cancer Therapy Start Date
    derivation:
      least:
        sources: [CMNACTDT, PRNACTDT]

  # Day 1 is TRTSDT itself.
  - name: NACTDY
    type: int
    label: New Anti-Cancer Therapy Start Relative Day
    derivation:
      study_day:
        date: NACTDT
        reference: TRTSDT

  - name: NACTFL
    type: str
    label: New Anti-Cancer Therapy Flag
    derivation:
      flag: "NACTDT IS NOT NULL"
    verifications:
      allowed_values:
        values: [Y]

verifications:
  - unique:
      columns: [STUDYID, USUBJID]
  - row_count:
      min: 7
      max: 7
  - all_or_none:
      id: therapy-date-day-and-flag-agree
      columns: [NACTDT, NACTDY, NACTFL]
  - implies:
      id: new-therapy-starts-on-or-after-treatment-start
      when: "NACTFL = 'Y'"
      then: "NACTDT >= TRTSDT"
