schema_version: "1.0"
domain: ADSL
keys: [STUDYID, USUBJID]
input:
  ADSL_RAW: input/adsl.csv
  CM: input/cm.csv
  PR: input/pr.csv
base: ADSL_RAW

output:
  path: adsl.csv
  columns: [STUDYID, USUBJID, TRTSDT, NACTDT, NACTDY, NACTFL]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation: ADSL_RAW.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: ADSL_RAW.USUBJID

  - name: TRTSDT
    type: date
    label: Date of First Exposure to Treatment
    derivation: ADSL_RAW.TRTSDT
    verifications:
      not_missing: {}

  - name: CMNACTDT
    type: date
    label: First New Anti-Cancer Medication Date
    derivation:
      aggregate:
        filter: "CM.CMCAT = 'ON TREATMENT' AND CM.CMSTDTC IS NOT NULL"
        expr: "MIN(CM.CMSTDTC)"

  - name: PRNACTDT
    type: date
    label: First New Anti-Cancer Procedure Date
    derivation:
      aggregate:
        filter: "PR.PRCAT = 'CANCER RELATED' AND PR.PRSCAT = 'ON TREATMENT' AND PR.PRSTDTC IS NOT NULL"
        expr: "MIN(PR.PRSTDTC)"

  - name: NACTDT
    type: date
    label: New Anti-Cancer Therapy Start Date
    derivation:
      least:
        sources: [CMNACTDT, PRNACTDT]

  - name: NACTDY
    type: int
    label: New Anti-Cancer Therapy Start Relative Day
    derivation:
      study_day:
        date: NACTDT
        reference: TRTSDT

  - name: NACTFL
    type: str
    label: New Anti-Cancer Therapy Flag
    derivation:
      case:
        - when: "NACTDT IS NOT NULL"
          then:
            literal: Y
    verifications:
      allowed_values:
        values: [Y]

verifications:
  - unique:
      columns: [STUDYID, USUBJID]
  - row_count:
      min: 5
      max: 5
  - all_or_none:
      id: therapy-date-day-and-flag-agree
      columns: [NACTDT, NACTDY, NACTFL]
  - implies:
      id: new-therapy-starts-on-or-after-treatment-start
      when: "NACTFL = 'Y'"
      then: "NACTDT >= TRTSDT"
