schema_version: "1.0"
domain: ADEX
keys: [STUDYID, USUBJID, EXTRT]
input:
  TRT: input/subject_treatment.csv
  EX: {path: input/ex.csv, types: {EXDOSE: float, EXDOSEN: float}}
base: TRT

output:
  path: adex.csv
  columns: [STUDYID, USUBJID, EXTRT, DOSECUM]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation: TRT.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: TRT.USUBJID

  - name: EXTRT
    type: str
    label: Name of Treatment
    derivation: TRT.EXTRT

  - name: DOSECUM
    type: float
    label: Cumulative Dose
    derivation:
      aggregate: "SUM(EX.EXDOSE)"
