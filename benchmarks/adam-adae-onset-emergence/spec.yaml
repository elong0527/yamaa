schema_version: "1.0"
domain: ADAE
keys: [STUDYID, USUBJID, AESEQ]
input:
  AE: input/ae.csv
  ADSL: input/adsl.csv
base: AE

output:
  path: adae.csv
  columns: [STUDYID, USUBJID, AESEQ, AETERM, ASTDTM, TRTSDTM, TRTEMFL, AOCCFL]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation: AE.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: AE.USUBJID

  - name: AESEQ
    type: int
    label: Sequence Number
    derivation: AE.AESEQ

  - name: AETERM
    type: str
    label: Reported Term for the Adverse Event
    derivation: AE.AETERM

  - name: ASTDTM
    type: datetime
    label: Analysis Start Date/Time
    derivation: AE.AESTDTC

  - name: TRTSDTM
    type: datetime
    label: Datetime of First Exposure to Treatment
    derivation: ADSL.TRTSDTM

  # A comparison with a missing moment is unknown, never true, so the flag
  # stays empty when either the onset or the first exposure is missing.
  - name: TRTEMFL
    type: str
    label: Treatment Emergent Analysis Flag
    derivation:
      case:
        - when: "ASTDTM >= TRTSDTM"
          then: {literal: Y}
    verifications:
      allowed_values:
        values: [Y]

  - name: AOCCSEQ
    type: int
    label: Subject Occurrence Sequence Number
    derivation:
      row_number:
        window:
          group_by: [STUDYID, USUBJID]
          order_by: [ASTDTM, AESEQ]
          filter: "TRTEMFL = 'Y'"

  - name: AOCCFL
    type: str
    label: First Occurrence Within Subject Flag
    derivation:
      case:
        - when: "AOCCSEQ = 1"
          then: {literal: Y}
    verifications:
      allowed_values:
        values: [Y]

verifications:
  - unique:
      columns: [STUDYID, USUBJID, AESEQ]
  - row_count:
      min: 7
      max: 7
  - implies:
      id: treatment-emergent-onset-is-not-before-first-dose
      when: "TRTEMFL = 'Y'"
      then: "ASTDTM >= TRTSDTM"
  - implies:
      id: occurrence-flag-requires-treatment-emergence
      when: "AOCCFL = 'Y'"
      then: "TRTEMFL = 'Y'"
