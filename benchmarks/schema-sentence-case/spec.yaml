schema_version: "1.0"
domain: ADLB
keys: [STUDYID, USUBJID]
input:
  LB: input/lb.csv

output:
  path: adlb.csv
  columns:
    - STUDYID
    - USUBJID
    - VISIT
    - AVISIT
    - AVISIT_TITLE
  order_by: [VISIT, USUBJID]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation: LB.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: LB.USUBJID

  - name: VISIT
    type: str
    label: Visit Name
    derivation: LB.VISIT

  # First character up, every later ASCII letter down:
  # END OF TREATMENT -> End of treatment.
  - name: AVISIT
    type: str
    label: Analysis Visit
    derivation:
      str_sentence:
        source: LB.VISIT

  # Each run of ASCII letters starts uppercase, so a hyphen starts a new
  # word: FOLLOW-UP -> Follow-Up, where sentence case gives Follow-up.
  - name: AVISIT_TITLE
    type: str
    label: Analysis Visit Title Case
    derivation:
      str_title:
        source: LB.VISIT
