schema_version: "1.0"
domain: SUPPDM
keys: [STUDYID, RDOMAIN, USUBJID, IDVAR, IDVARVAL, QNAM]

input:
  ODM: input/odm.csv

output:
  path: suppdm.csv
  columns: [STUDYID, RDOMAIN, USUBJID, IDVAR, IDVARVAL, QNAM, QLABEL, QVAL, QORIG, QEVAL]
  order_by: [STUDYID, RDOMAIN, USUBJID, IDVAR, IDVARVAL, QNAM]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier

  - name: RDOMAIN
    type: str
    label: Related Domain Abbreviation
    derivation:
      literal: DM

  - name: USUBJID
    type: str
    label: Unique Subject Identifier

  - name: IDVAR
    type: str
    label: Identifying Variable
    derivation:
      literal: USUBJID

  - name: IDVARVAL
    type: str
    label: Identifying Variable Value
    derivation: USUBJID
    verifications:
      not_missing: {}

  - name: RACEMIN
    type: str
    label: Alphabetically First Reported Race

  - name: RACESINGLE
    type: str
    label: Reported Race When One Race Was Reported

  - name: RACECOUNT
    type: int
    label: Number of Distinct Reported Races

  - name: QNAM
    type: str
    label: Qualifier Variable Name
    verifications:
      allowed_values:
        values: [RACE1, RACE2]

  - name: QLABEL
    type: str
    label: Qualifier Label

  - name: QVALRAW
    type: str
    label: Collected Race

  - name: QVAL
    type: str
    label: Data Value
    derivation:
      mapping:
        source: QVALRAW
        dict:
          "American Indian or Alaska Native": AMERICAN INDIAN OR ALASKA NATIVE
          "Asian": ASIAN
          "Black or African American": BLACK OR AFRICAN AMERICAN
          "Native Hawaiian or Other Pacific Islander": NATIVE HAWAIIAN OR OTHER PACIFIC ISLANDER
          "White": WHITE
          "Other, specify: Fijian": OTHER
          "Not reported": NOT REPORTED
          "Subject refused": UNKNOWN
        missing: null
    verifications:
      - not_missing: {}
      - allowed_values:
          values: [AMERICAN INDIAN OR ALASKA NATIVE, ASIAN,
            BLACK OR AFRICAN AMERICAN,
            NATIVE HAWAIIAN OR OTHER PACIFIC ISLANDER, WHITE,
            OTHER, NOT REPORTED, UNKNOWN]

  - name: QORIG
    type: str
    label: Origin
    derivation:
      literal: CRF

  - name: QEVAL
    type: str
    label: Evaluator
    derivation:
      literal: null

rows:
  - id: first_reported_race
    group_by: [ODM.StudyOID, ODM.SubjectKey]
    filter: "RACEMIN < RACESINGLE"
    derivations:
      STUDYID: ODM.StudyOID
      USUBJID: ODM.SubjectKey
      RACEMIN:
        aggregate:
          filter: "ODM.ItemOID = 'IT.DM.RACE'"
          expr: "MIN(ODM.Value)"
      RACESINGLE:
        aggregate:
          filter: "ODM.ItemOID = 'IT.DM.RACE'"
          expr: "MAX(ODM.Value)"
      # The language has no COUNT(DISTINCT ...): count distinct races from the
      # MIN/MAX distinctness signal instead, so duplicated identical rows do
      # not inflate the count.
      RACECOUNT:
        case:
          - when: "RACEMIN = RACESINGLE"
            then:
              literal: 1
          - otherwise:
              literal: 2
      QVALRAW:
        aggregate:
          filter: "ODM.ItemOID = 'IT.DM.RACE'"
          expr: "MIN(ODM.Value)"
      QNAM:
        literal: RACE1
      QLABEL:
        literal: Race 1

  - id: second_reported_race
    group_by: [ODM.StudyOID, ODM.SubjectKey]
    filter: "RACEMIN < RACESINGLE"
    derivations:
      STUDYID: ODM.StudyOID
      USUBJID: ODM.SubjectKey
      RACEMIN:
        aggregate:
          filter: "ODM.ItemOID = 'IT.DM.RACE'"
          expr: "MIN(ODM.Value)"
      RACESINGLE:
        aggregate:
          filter: "ODM.ItemOID = 'IT.DM.RACE'"
          expr: "MAX(ODM.Value)"
      # The language has no COUNT(DISTINCT ...): count distinct races from the
      # MIN/MAX distinctness signal instead, so duplicated identical rows do
      # not inflate the count.
      RACECOUNT:
        case:
          - when: "RACEMIN = RACESINGLE"
            then:
              literal: 1
          - otherwise:
              literal: 2
      QVALRAW:
        aggregate:
          filter: "ODM.ItemOID = 'IT.DM.RACE'"
          expr: "MAX(ODM.Value)"
      QNAM:
        literal: RACE2
      QLABEL:
        literal: Race 2

verifications:
  - unique:
      columns: [STUDYID, RDOMAIN, USUBJID, IDVAR, IDVARVAL, QNAM]
  - row_count:
      min: 4
      max: 4
  - assert:
      id: race-count-within-scope
      expr: "RACECOUNT <= 2"
  - implies:
      id: race-value-has-parent-identifier
      when: "QVAL IS NOT NULL"
      then: "RDOMAIN IS NOT NULL AND IDVAR IS NOT NULL AND IDVARVAL IS NOT NULL"
