schema_version: "1.0"
domain: ADEX
keys: [STUDYID, USUBJID, EXTRT]
input:
  TRT: input/subject_treatment.csv
  EX: {path: input/ex.csv, types: {EXDOSE: float}}
base: TRT

output:
  path: adex.csv
  columns: [STUDYID, USUBJID, EXTRT, EXDOSU, DOSECUM, NCYCLES, RDI]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation: TRT.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: TRT.USUBJID

  - name: EXTRT
    type: str
    label: Name of Treatment
    derivation: TRT.EXTRT

  - name: EXDOSU
    type: str
    label: Dose Units
    derivation: TRT.EXDOSU

  - name: DOSECUM
    type: float
    label: Cumulative Dose
    derivation:
      aggregate:

        expr: "SUM(EX.EXDOSE)"
  - name: NCYCLES
    type: int
    label: Number of Treatment Cycles
    derivation:
      aggregate:

        expr: "COUNT(EX.EXSEQ)"
  - name: PLANDOSE
    type: float
    label: Planned Dose per Cycle
    derivation: TRT.PLANDOSE

  - name: PLANCYC
    type: int
    label: Planned Number of Cycles
    derivation: TRT.PLANCYC

  - name: RDI
    type: float
    label: Relative Dose Intensity
    derivation:
      compute:
        expr: "100 * DOSECUM / NULLIF(PLANDOSE * PLANCYC, 0)"

verifications:
  - row_count:
      min: 6
      max: 6
