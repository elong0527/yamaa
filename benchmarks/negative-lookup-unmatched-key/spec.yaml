schema_version: "1.0"
domain: LB
keys: [STUDYID, USUBJID, LBSEQ]
input:
  LB_RAW: input/lb_raw.csv
  LBRANGE: input/lbrange.csv
base: LB_RAW
intermediates:
  # The limit for the result's test code and sex. strict: a result whose
  # test code and sex match no table row stops the run.
  - id: REFRANGE
    dataset: LBRANGE
    key: [LBTESTCD, SEX]
    strict: true

output:
  path: lb.csv
  columns: [STUDYID, USUBJID, LBSEQ, LBTESTCD, SEX, LBSTNRHI]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation: LB_RAW.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: LB_RAW.USUBJID

  - name: LBSEQ
    type: int
    label: Sequence Number
    derivation: LB_RAW.LBSEQ

  - name: LBTESTCD
    type: str
    label: Lab Test or Examination Short Name
    derivation: LB_RAW.LBTESTCD

  - name: SEX
    type: str
    label: Sex
    derivation: LB_RAW.SEX

  - name: LBSTNRHI
    type: float
    label: Reference Range Upper Limit-Std Units
    derivation: REFRANGE.NRHI
