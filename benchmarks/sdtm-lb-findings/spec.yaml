schema_version: "1.0"
domain: LB
keys: [STUDYID, USUBJID, LBSEQ]
input:
  ODM: input/odm.csv

output:
  path: lb.csv
  columns: [DOMAIN, STUDYID, USUBJID, LBSEQ, LBTESTCD, LBTEST, LBORRES, LBORRESU, LBSTRESN, LBSTRESU, LBDTC]

columns:
  - name: DOMAIN
    type: str
    label: Domain Abbreviation
    derivation: {literal: LB}

  - name: STUDYID
    type: str
    label: Study Identifier
    derivation: ODM.StudyOID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: ODM.SubjectKey

  - name: LBTESTCD
    type: str
    label: Lab Test or Examination Short Name

  - name: LBTEST
    type: str
    label: Lab Test or Examination Name

  - name: LBORRES
    type: str
    label: Result or Finding in Original Units

  - name: LBORRESU
    type: str
    label: Original Units

  - name: LBSTRESN
    type: float
    label: Numeric Result/Finding in Standard Units

  - name: LBSTRESU
    type: str
    label: Standard Units

  - name: LBDTC
    type: date
    label: Date/Time of Specimen Collection

  # Numbered within each subject by collection date, then test code.
  - name: LBSEQ
    type: int
    label: Sequence Number
    derivation:
      row_number:
        window:
          group_by: [STUDYID, USUBJID]
          order_by: [LBDTC, LBTESTCD]

rows:
  # One record per subject and visit with a calcium entry, dated by the
  # visit's collection date item; ONLY() fails if the visit carries either
  # item more than once. An entry that is not a number, such as NOT DONE,
  # gives a missing LBSTRESN.
  - id: calcium
    group_by: [ODM.StudyOID, ODM.SubjectKey, ODM.StudyEventOID]
    filter: "LBORRES IS NOT NULL"
    derivations:
      LBTESTCD: {literal: CA}
      LBTEST: {literal: Calcium}
      LBORRES:
        aggregate:
          filter: "ODM.ItemOID = 'IT.LB.CALCIUM'"
          expr: "ONLY(ODM.Value)"
      LBDTC:
        aggregate:
          filter: "ODM.ItemOID = 'IT.LB.LBDTC'"
          expr: "ONLY(ODM.Value)"
      LBORRESU: {literal: mg/dL}
      LBSTRESN:
        value: {source: LBORRES}
        missing: null
      LBSTRESU: {literal: mg/dL}

  # The same for the creatinine entry.
  - id: creatinine
    group_by: [ODM.StudyOID, ODM.SubjectKey, ODM.StudyEventOID]
    filter: "LBORRES IS NOT NULL"
    derivations:
      LBTESTCD: {literal: CREAT}
      LBTEST: {literal: Creatinine}
      LBORRES:
        aggregate:
          filter: "ODM.ItemOID = 'IT.LB.CREAT'"
          expr: "ONLY(ODM.Value)"
      LBDTC:
        aggregate:
          filter: "ODM.ItemOID = 'IT.LB.LBDTC'"
          expr: "ONLY(ODM.Value)"
      LBORRESU: {literal: mg/dL}
      LBSTRESN:
        value: {source: LBORRES}
        missing: null
      LBSTRESU: {literal: mg/dL}
