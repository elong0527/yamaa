schema_version: "1.0"
domain: ADSL
keys: [STUDYID, USUBJID]
input:
  DM: input/dm.parquet

output:
  path: adsl.parquet
  columns: [STUDYID, USUBJID, AGE]
  violation_log: adsl-violations.parquet

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation: DM.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: DM.USUBJID

  - name: AGE
    type: int
    label: Age
    derivation: DM.AGE
    verifications:
      - range:
          min: 18
          max: 100
          severity: warning
