schema_version: "1.0"
domain: ADAE
keys: [STUDYID, USUBJID, AESEQ]
input:
  AE: input/ae.csv

output:
  path: adae.csv
  columns: [STUDYID, USUBJID, AESEQ, AETERM, ASTDTM]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation: AE.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation: AE.USUBJID

  - name: AESEQ
    type: int
    label: Sequence Number
    derivation: AE.AESEQ

  - name: AETERM
    type: str
    label: Reported Term for the Adverse Event
    derivation: AE.AETERM

  # A datetime is a site-clock reading with no zone or offset. Start text
  # carrying either (Z, +02:00) is not a datetime, and nothing replaces a
  # failed conversion, so that text stops the run.
  - name: ASTDTM
    type: datetime
    label: Analysis Start Date/Time
    derivation: AE.AESTDTC
