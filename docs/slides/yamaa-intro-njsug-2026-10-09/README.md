# yamaa-intro-njsug-2026-10-09 slides

"yamaa: a language-agnostic YAML schema to map and derive CDISC data from
ODM to SDTM and ADaM" -- an introduction to yamaa for NJSUG (2026-10-09).

[View the rendered slides](https://elong0527.github.io/yamaa/slides/yamaa-intro-njsug-2026-10-09/index.html)

## Files

- `index.qmd` -- the deck (diagram-first: short bullets, one real spec excerpt)
- `custom.css` -- the deck's single style file (copied from yamaa-pilot7-2026-09-25)

## Build

The HTML is built by the Deploy docs GitHub Action and published with the
docs site. To preview locally: `quarto render index.qmd` from this folder.
The output is one self-contained HTML file (`embed-resources: true`).
