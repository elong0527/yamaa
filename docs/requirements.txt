# Pinned dependencies for building the YAMAA documentation site with
# MkDocs Material. Install alongside the example-dashboard dependencies:
#
#   pip install -r docs/requirements.txt -r .github/scripts/example-docs/requirements.txt
#
# The example-dashboard pins (markdown-it-py, mdurl, PyYAML) live in
# .github/scripts/example-docs/requirements.txt and are intentionally not
# duplicated here.
mkdocs==1.6.1
mkdocs-material==9.7.7
