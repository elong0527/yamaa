# Pinned dependencies for building the yamaa documentation site with
# MkDocs Material. Install alongside the benchmark-dashboard dependencies:
#
#   pip install -r docs/requirements.txt -r .github/scripts/benchmark-docs/requirements.txt
#
# The benchmark-dashboard pins (markdown-it-py, mdurl, PyYAML) live in
# .github/scripts/benchmark-docs/requirements.txt and are intentionally not
# duplicated here.
mkdocs==1.6.1
mkdocs-material==9.7.7
