registry_version: "1.0.0"
namespace: collision
specification_versions: ["1.0"]

entries:
  - canonical_name: FIRST
    aliases: [SHARED]
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: x, types: [int, float]}
      min_arity: 1
      max_arity: 1
    type_promotion: preserve_numeric
    result_type: promoted_numeric
    missing_values: propagate
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: exact_or_binary64
    accuracy:
      mode: exact_or_binary64
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: Conformance-only identity contract.

  - canonical_name: SHARED
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: x, types: [int, float]}
      min_arity: 1
      max_arity: 1
    type_promotion: preserve_numeric
    result_type: promoted_numeric
    missing_values: propagate
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: exact_or_binary64
    accuracy:
      mode: exact_or_binary64
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: Conformance-only colliding identity contract.
