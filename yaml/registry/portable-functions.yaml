registry_version: "1.0.0"
namespace: core
specification_versions: ["1.0"]

entries:
  - canonical_name: ABS
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: x, types: [int, float]}
      min_arity: 1
      max_arity: 1
    type_promotion: preserve_numeric
    result_type: promoted_numeric
    missing_values: propagate
    failures:
      domain: [integer_minimum]
      overflow: fail
      non_finite_result: fail
    determinism: exact_or_binary64
    accuracy:
      mode: exact_or_binary64
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: Absolute value of x.

  - canonical_name: CEIL
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: x, types: [int, float]}
      min_arity: 1
      max_arity: 1
    type_promotion: always_float
    result_type: float
    missing_values: propagate
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: binary64
    accuracy:
      mode: exact
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: Least integral value not less than x, represented as float.

  - canonical_name: FLOOR
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: x, types: [int, float]}
      min_arity: 1
      max_arity: 1
    type_promotion: always_float
    result_type: float
    missing_values: propagate
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: binary64
    accuracy:
      mode: exact
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: Greatest integral value not greater than x, represented as float.

  - canonical_name: TRUNC
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: x, types: [int, float]}
      min_arity: 1
      max_arity: 1
    type_promotion: always_float
    result_type: float
    missing_values: propagate
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: binary64
    accuracy:
      mode: exact
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: Integral part of x toward zero, represented as float.

  - canonical_name: SQRT
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: x, types: [int, float]}
      min_arity: 1
      max_arity: 1
    type_promotion: always_float
    result_type: float
    missing_values: propagate
    failures:
      domain: [negative_argument]
      overflow: fail
      non_finite_result: fail
    determinism: binary64
    accuracy:
      mode: binary64
      absolute_tolerance: 1.0e-15
      relative_tolerance: 1.0e-15
    availability: {since: "1.0", deprecated: null}
    definition: Non-negative square root of x.

  - canonical_name: POWER
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: x, types: [int, float]}
        - {name: "y", types: [int, float]}
      min_arity: 2
      max_arity: 2
    type_promotion: always_float
    result_type: float
    missing_values: propagate
    failures:
      domain: [zero_to_negative_power, negative_to_fractional_power]
      overflow: fail
      non_finite_result: fail
    determinism: binary64
    accuracy:
      mode: binary64
      absolute_tolerance: 1.0e-15
      relative_tolerance: 1.0e-15
    availability: {since: "1.0", deprecated: null}
    definition: x raised to the power y.

  - canonical_name: EXP
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: x, types: [int, float]}
      min_arity: 1
      max_arity: 1
    type_promotion: always_float
    result_type: float
    missing_values: propagate
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: binary64
    accuracy:
      mode: binary64
      absolute_tolerance: 1.0e-15
      relative_tolerance: 1.0e-15
    availability: {since: "1.0", deprecated: null}
    definition: The base of natural logarithms raised to x.

  - canonical_name: LN
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: x, types: [int, float]}
      min_arity: 1
      max_arity: 1
    type_promotion: always_float
    result_type: float
    missing_values: propagate
    failures:
      domain: [non_positive_argument]
      overflow: fail
      non_finite_result: fail
    determinism: binary64
    accuracy:
      mode: binary64
      absolute_tolerance: 1.0e-15
      relative_tolerance: 1.0e-15
    availability: {since: "1.0", deprecated: null}
    definition: Natural logarithm of x.

  - canonical_name: MOD
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: x, types: [int, float]}
        - {name: "y", types: [int, float]}
      min_arity: 2
      max_arity: 2
    type_promotion: promote_numeric
    result_type: promoted_numeric
    missing_values: propagate
    failures:
      domain: [zero_divisor]
      overflow: fail
      non_finite_result: fail
    determinism: exact_or_binary64
    accuracy:
      mode: exact_or_binary64
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: Remainder of x divided by y, with the sign of x.

  - canonical_name: GREATEST
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: value, types: [int, float], variadic: true}
      min_arity: 2
      max_arity: null
    type_promotion: promote_numeric
    result_type: promoted_numeric
    missing_values: ignore_missing_all_missing
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: exact_or_binary64
    accuracy:
      mode: exact_or_binary64
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: Largest non-missing argument, or missing when all are missing.

  - canonical_name: LEAST
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: value, types: [int, float], variadic: true}
      min_arity: 2
      max_arity: null
    type_promotion: promote_numeric
    result_type: promoted_numeric
    missing_values: ignore_missing_all_missing
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: exact_or_binary64
    accuracy:
      mode: exact_or_binary64
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: Smallest non-missing argument, or missing when all are missing.

  - canonical_name: NULLIF
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: x, types: [int, float]}
        - {name: "y", types: [int, float]}
      min_arity: 2
      max_arity: 2
    type_promotion: promote_numeric
    result_type: promoted_numeric
    missing_values: null_if_equal
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: exact_or_binary64
    accuracy:
      mode: exact_or_binary64
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: Missing when non-missing x and y are equal; otherwise x.

  - canonical_name: COALESCE
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: value, types: [int, float], variadic: true}
      min_arity: 1
      max_arity: null
    type_promotion: promote_numeric
    result_type: promoted_numeric
    missing_values: first_non_missing
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: exact_or_binary64
    accuracy:
      mode: exact_or_binary64
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: First non-missing argument, or missing when all are missing.

  - canonical_name: NORMAL_CDF
    aliases: []
    evaluation_kind: scalar
    signature:
      parameters:
        - {name: x, types: [int, float]}
      min_arity: 1
      max_arity: 1
    type_promotion: always_float
    result_type: float
    missing_values: propagate
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: binary64
    accuracy:
      mode: absolute_or_relative
      absolute_tolerance: 0.0
      relative_tolerance: 1.0e-12
    availability: {since: "1.0", deprecated: null}
    definition: >-
      Standard normal cumulative probability at x, mathematically one half
      times erfc of negative x divided by the square root of two. Finite tail
      results may round to exactly zero or one under the accuracy contract.

  - canonical_name: SUM
    aliases: []
    evaluation_kind: reducer
    signature:
      parameters:
        - {name: value, types: [int, float]}
      min_arity: 1
      max_arity: 1
    type_promotion: preserve_numeric
    result_type: input_numeric
    missing_values: ignore_missing_all_missing
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: order_independent
    accuracy:
      mode: exact_or_binary64
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: Total of non-missing values, or missing when all are missing.

  - canonical_name: COUNT
    aliases: []
    evaluation_kind: reducer
    signature:
      parameters:
        - name: value
          types: [str, int, float, bool, date, datetime, record_star]
      min_arity: 1
      max_arity: 1
    type_promotion: count
    result_type: int
    missing_values: count_non_missing_or_records
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: order_independent
    accuracy:
      mode: exact
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: >-
      Count of non-missing values, or record count for the qualified star form.

  - canonical_name: MIN
    aliases: []
    evaluation_kind: reducer
    signature:
      parameters:
        - {name: value, types: [str, int, float, date, datetime]}
      min_arity: 1
      max_arity: 1
    type_promotion: preserve_input
    result_type: input
    missing_values: ignore_missing_all_missing
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: order_independent
    accuracy:
      mode: exact
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: Smallest non-missing value, or missing when all are missing.

  - canonical_name: MAX
    aliases: []
    evaluation_kind: reducer
    signature:
      parameters:
        - {name: value, types: [str, int, float, date, datetime]}
      min_arity: 1
      max_arity: 1
    type_promotion: preserve_input
    result_type: input
    missing_values: ignore_missing_all_missing
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: order_independent
    accuracy:
      mode: exact
      absolute_tolerance: 0.0
      relative_tolerance: 0.0
    availability: {since: "1.0", deprecated: null}
    definition: Largest non-missing value, or missing when all are missing.

  - canonical_name: MEAN
    aliases: []
    evaluation_kind: reducer
    signature:
      parameters:
        - {name: value, types: [int, float]}
      min_arity: 1
      max_arity: 1
    type_promotion: always_float
    result_type: float
    missing_values: ignore_missing_all_missing
    failures:
      domain: []
      overflow: fail
      non_finite_result: fail
    determinism: order_independent
    accuracy:
      mode: binary64
      absolute_tolerance: 1.0e-15
      relative_tolerance: 1.0e-15
    availability: {since: "1.0", deprecated: null}
    definition: >-
      SUM of non-missing values divided by their COUNT, or missing when all
      values are missing. SUM overflow fails before division.
