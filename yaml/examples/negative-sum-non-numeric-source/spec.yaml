schema_version: "1.0"
domain: ADAE
datasets:
  AE: input/ae.csv
base: AE
keys: [STUDYID, USUBJID, AESEQ]

output:
  columns: [STUDYID, USUBJID, AESEQ, ASEV, SEVTOT]

columns:
  - name: STUDYID
    type: str
    derivation:
      source: AE.STUDYID

  - name: USUBJID
    type: str
    derivation:
      source: AE.USUBJID

  - name: AESEQ
    type: int
    derivation:
      source: AE.AESEQ

  - name: ASEV
    type: str
    derivation:
      source: AE.AESEV

  - name: SEVTOT
    type: float
    derivation:
      aggregate:
        group_by: [STUDYID, USUBJID]
        expr: "SUM(ASEV)"
