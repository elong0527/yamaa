schema_version: "1.0"
domain: ADSL
datasets:
  DM: input/dm.csv
  EX: input/ex.csv
base: DM
keys: [STUDYID, USUBJID]

record_lookups:
  - id: EX
    dataset: EX
    order_by: [EX.EXSTDTC, EX.EXSEQ]
    keep: first

output:
  columns: [STUDYID, USUBJID, TRT01A]

columns:
  - name: STUDYID
    type: str
    derivation:
      source: DM.STUDYID

  - name: USUBJID
    type: str
    derivation:
      source: DM.USUBJID

  - name: TRT01A
    type: str
    derivation:
      source: EX.EXTRT
