schema_version: "1.0"
domain: ADSL
datasets:
  DM: input/dm.csv
base: DM
keys: [STUDYID, USUBJID]

output:
  columns: [STUDYID, USUBJID, SUBJREF]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation:
      source: DM.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation:
      source: DM.USUBJID

  - name: SITEID
    type: str
    label: Study Site Identifier
    derivation:
      source: DM.SITEID

  - name: SUBJID
    type: str
    label: Subject Identifier for the Study
    derivation:
      source: DM.SUBJID

  - name: SUBJREF
    type: str
    label: Subject Display Reference
    derivation:
      str_template: "{SITEID + ':' + SUBJID}"
