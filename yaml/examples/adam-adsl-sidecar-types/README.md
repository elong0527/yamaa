# ADaM ADSL: carry typed randomization values from a source schema

This example uses demographics with a portable source schema to produce one
record per subject:

- `RANDDT` is the subject's randomization date;
- `RANDDTTM` is the subject's local randomization date and time, with an
  omitted second represented as second zero.
