# ADaM ADSL: record randomization timing

This example uses demographics to produce one record per subject:

- `RANDDT` is the subject's randomization date;
- `RANDDY` is the randomization study day relative to the subject reference
  start date;
- `RANDDTC` is the subject's local randomization date and time, rendered to
  whole-second precision.
