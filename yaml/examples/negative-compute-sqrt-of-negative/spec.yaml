schema_version: "1.0"
domain: ADSL
datasets:
  DM: input/dm.csv
base: DM
keys: [STUDYID, USUBJID]

output:
  columns: [STUDYID, USUBJID, BSA]

columns:
  - name: STUDYID
    type: str
    derivation:
      source: DM.STUDYID

  - name: USUBJID
    type: str
    derivation:
      source: DM.USUBJID

  - name: HEIGHTCM
    type: float
    derivation:
      source: DM.HEIGHTCM

  - name: WEIGHTKG
    type: float
    derivation:
      source: DM.WEIGHTKG

  - name: BSA
    type: float
    derivation:
      compute:
        expr: "SQRT(HEIGHTCM * WEIGHTKG / 3600)"
