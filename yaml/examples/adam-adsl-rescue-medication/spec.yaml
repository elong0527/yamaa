schema_version: "1.0"
domain: ADSL
datasets:
  DM: input/dm.csv
  CM: {path: input/cm.csv, types: {CMSEQ: int}}
base: DM
keys: [STUDYID, USUBJID]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation:
      source: DM.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation:
      source: DM.USUBJID

  - name: RESCTRT
    type: str
    label: Rescue Medication
    derivation:
      source:
        variable: CM.CMTRT
        multiple_matches:
          order_by: [CM.CMSTDTC, CM.CMSEQ]
          keep: first
          filter: "CM.CMCAT = 'RESCUE MEDICATION'"

verifications:
  - row_count:
      min: 2
      max: 2
