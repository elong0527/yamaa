schema_version: "1.0"
domain: ADSL
datasets:
  DM: {path: input/dm.csv}
base: DM
keys: [STUDYID, USUBJID]

output:
  path: adsl.csv
  columns: [STUDYID, USUBJID, SITENM]

columns:
  - name: STUDYID
    type: str
    label: Study Identifier
    derivation:
      source: DM.STUDYID

  - name: USUBJID
    type: str
    label: Unique Subject Identifier
    derivation:
      source: DM.USUBJID

  - name: SITENM
    type: str
    label: Study Site Name
    derivation:
      source: DM.SITENM
