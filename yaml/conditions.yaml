version: "1.0"

conditions:
  aggregate_identifier_not_grouped: {rules: [R013], phases: [validation]}
  aggregate_multiple_records: {rules: [R013], phases: [derivation, row_construction]}
  all_or_none_failed: {rules: [R009], phases: [verification]}
  allowed_values_failed: {rules: [R009], phases: [verification]}
  ambiguous_baseline: {rules: [R007], phases: [derivation]}
  ambiguous_dictionary: {rules: [R007], phases: [validation]}
  artifact_path_collision: {rules: [R020], phases: [validation]}
  assert_failed: {rules: [R009], phases: [verification]}
  conversion_failed: {rules: [R011], phases: [convert]}
  correlated_filter_with_unique_verification: {rules: [R003], phases: [validation]}
  decimals_not_applicable: {rules: [R020], phases: [validation]}
  dependency_cycle: {rules: [R001], phases: [validation]}
  division_by_zero: {rules: [R010], phases: [derivation]}
  duplicate_identifier: {rules: [R002, R003], phases: [validation]}
  duplicate_intermediate_records: {rules: [R003], phases: [verification]}
  duplicate_key: {rules: [R005], phases: [output]}
  duplicate_order_term: {rules: [R005], phases: [validation]}
  field_parse_failed: {rules: [R014], phases: [ingest]}
  forward_reference: {rules: [R001], phases: [validation]}
  function_contract_mismatch: {rules: [R018], phases: [validation]}
  implication_failed: {rules: [R009], phases: [verification]}
  incomparable_range_types: {rules: [R003], phases: [validation]}
  incomparable_sources: {rules: [R007], phases: [validation]}
  incompatible_input_type: {rules: [R004, R007, R010, R013, R016], phases: [validation]}
  inheritance_cycle: {rules: [R017], phases: [validation]}
  integer_overflow: {rules: [R010], phases: [derivation]}
  internal_column_in_keys: {rules: [R005], phases: [validation]}
  invalid_aggregate_context: {rules: [R013], phases: [validation]}
  invalid_calendar_date: {rules: [R016], phases: [impute]}
  invalid_clear: {rules: [R017], phases: [validation]}
  invalid_date_text: {rules: [R016], phases: [impute]}
  invalid_datetime_text: {rules: [R016], phases: [impute]}
  invalid_field_type: {rules: [R006], phases: [validation]}
  invalid_mapping_dictionary: {rules: [R007], phases: [validation]}
  invalid_parent_path: {rules: [R017], phases: [validation]}
  invalid_predicate: {rules: [R004], phases: [validation]}
  invalid_regex: {rules: [R022], phases: [validation]}
  invalid_string_template: {rules: [R012], phases: [validation]}
  invalid_text: {rules: [R019], phases: [ingest]}
  key_dependency: {rules: [R001], phases: [validation]}
  length_failed: {rules: [R009], phases: [verification]}
  ln_of_nonpositive: {rules: [R010], phases: [derivation]}
  mapping_dictionary_source_conflict: {rules: [R007], phases: [validation]}
  matches_failed: {rules: [R009], phases: [verification]}
  missing_input: {rules: [R008], phases: [cut, mapping]}
  missing_key: {rules: [R005], phases: [output]}
  missing_required_field: {rules: [R006], phases: [validation]}
  missing_verification_id: {rules: [R009], phases: [validation]}
  missing_warning_log: {rules: [R009], phases: [validation]}
  month_not_permitted: {rules: [R016], phases: [validation]}
  month_out_of_range: {rules: [R016], phases: [validation]}
  month_required: {rules: [R016], phases: [validation]}
  multiple_matches: {rules: [R003, R008], phases: [join]}
  multiple_values_per_key: {rules: [R001], phases: [derivation]}
  no_applicable_keys: {rules: [R003], phases: [validation]}
  not_missing_failed: {rules: [R009], phases: [verification]}
  producer_contract_mismatch: {rules: [R014], phases: [validation]}
  prohibited_construct: {rules: [R003, R010, R013], phases: [validation]}
  prohibited_function: {rules: [R010, R013], phases: [validation]}
  publication_failed: {rules: [R020], phases: [output]}
  qualified_identifier: {rules: [R010], phases: [validation]}
  range_failed: {rules: [R009], phases: [verification]}
  redundant_field_type: {rules: [R014, R017], phases: [validation]}
  regex_group_out_of_range: {rules: [R022], phases: [validation]}
  rename_only_intermediate: {rules: [R003], phases: [validation]}
  resource_path_missing: {rules: [R021], phases: [validation]}
  resource_path_not_regular_file: {rules: [R021], phases: [validation]}
  resource_path_not_relative: {rules: [R021], phases: [validation]}
  resource_path_outside_project: {rules: [R021], phases: [validation]}
  resource_path_symlink: {rules: [R021], phases: [validation]}
  resource_path_uri_scheme: {rules: [R021], phases: [validation]}
  row_count_failed: {rules: [R009], phases: [verification]}
  schema_version_mismatch: {rules: [R017], phases: [validation]}
  source_field_name_duplicate: {rules: [R023, R027], phases: [ingest]}
  source_field_name_empty: {rules: [R023, R027], phases: [ingest]}
  source_field_type_unsupported: {rules: [R027], phases: [ingest]}
  source_field_value_invalid: {rules: [R027], phases: [ingest]}
  source_key_length_mismatch: {rules: [R003, R007], phases: [validation]}
  source_parquet_invalid: {rules: [R027], phases: [ingest]}
  source_profile_unknown: {rules: [R023], phases: [validation]}
  source_quote_unterminated: {rules: [R023], phases: [ingest]}
  source_record_width: {rules: [R023], phases: [ingest]}
  sqrt_of_negative: {rules: [R010], phases: [derivation]}
  undeclared_column: {rules: [R005], phases: [validation]}
  unique_failed: {rules: [R009], phases: [verification]}
  unknown_artifact_profile: {rules: [R020], phases: [validation]}
  unknown_field: {rules: [R002, R003, R004, R010, R013, R014], phases: [validation]}
  unmapped_key: {rules: [R008], phases: [mapping]}
  unmapped_value: {rules: [R008], phases: [mapping]}
  unmatched_key: {rules: [R003], phases: [join]}
  unpaired_fields: {rules: [R003], phases: [validation]}
  unresolvable_name: {rules: [R002, R004], phases: [validation]}
  unwritable_value: {rules: [R020], phases: [output]}
  value_not_permitted: {rules: [R009, R011, R016], phases: [validation]}
  window_on_window_result: {rules: [R007], phases: [validation]}
  window_order_by_required: {rules: [R007], phases: [validation]}
  zero_offset: {rules: [R007], phases: [validation]}
