version: "1.0"

expressions:
    str_extract:
        - source: {type: [variable, expression], required: true}
        - pattern: {type: str, required: true}
        - group: {type: int, required: false, default: 0}
        - unmapped: {type: expression, required: false}
