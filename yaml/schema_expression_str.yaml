version: "1.0"

expressions:
    str_extract:
        - source: {type: variable, required: true}
        - pattern: {type: str, required: true}
        - group: {type: int, required: false, default: 0}
        - missing: {type: literal_value, required: false}
        - no_match: {type: literal_value, required: false}
