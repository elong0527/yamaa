version: "1.0"

# R010 defines the closed numeric grammar, types, and failure conditions.

expressions:
    compute:
        - expr: {type: numeric_expression, required: true}

numeric_expression:
    type: str
    min_length: 1
