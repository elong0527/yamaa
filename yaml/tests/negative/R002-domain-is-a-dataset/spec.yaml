domain: DM
datasets:
  ODM: input/odm.csv
  DM: input/dm.csv
base: ODM
keys: [STUDYID, USUBJID]

columns:
  - name: STUDYID
    type: str
    derivation:
      source: ODM.StudyOID

  - name: USUBJID
    type: str
    derivation:
      source: ODM.SubjectKey
