domain: DM
datasets:
  ODM: input/odm.csv
base: ODM
keys: [STUDYID, USUBJID]

columns:
  - name: STUDYID
    type: str
    derivation:
      source: ODM.StudyOID

  - name: USUBJID
    type: str
    derivation:
      operations:
        - call:
            function: no_such_fn
